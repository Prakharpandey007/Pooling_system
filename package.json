{
  "name": "intervue",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client":"cd pooling_frontend && npm run dev",
    "server": "cd pooling_backend && npm start",
    "start":"concurrently \"npm run client\" \"npm run server\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
